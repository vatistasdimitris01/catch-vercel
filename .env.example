# Vercel Download Configuration
# Copy this file to .env and fill in your values
# Or just run the script with no config for interactive setup!

# Required: Your Vercel API token
# Get it from: https://vercel.com/account/tokens
VERCEL_TOKEN=your_token_here

# Optional: Deployment ID or "latest" (default: latest)
# Copy from your Vercel dashboard URL:
#   https://vercel.com/{scope}/{project}/{THIS_PART}/source
# Works with or without the dpl_ prefix
VERCEL_DEPLOYMENT=latest

# Optional: Project name (auto-detected from deployment ID)
# Only set this if auto-detection fails
# VERCEL_PROJECT=my-awesome-app

# Optional: Team slug or ID (auto-detected from deployment ID)
# Accepts either the slug (e.g. numanaral) or ID (e.g. team_xxx)
# Only set this if auto-detection fails
# VERCEL_TEAM=my-team

# Optional: Output directory (default: ./out)
# VERCEL_OUTPUT=./out
